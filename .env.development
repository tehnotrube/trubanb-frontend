# API base URL for local development outside the cluster
# When running inside the cluster (Docker/K8s), this is not needed -
# the app uses relative paths and Kong handles routing.
VITE_API_URL=http://localhost:30080
